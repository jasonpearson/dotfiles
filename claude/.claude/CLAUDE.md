## Always important

- be concise unless asked for detail
- when moving files tracked by git, prefer git mv over mv
- when creating git branches, always prefix with `jsonp--` and keep the total branch name to 36 characters or fewer
