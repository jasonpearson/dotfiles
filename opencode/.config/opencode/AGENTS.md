## git

- when working in a git worktree directory (often named like `.wt/`), always perform all operations inside the worktree directory, do not perform operations in the main repo directory
- when moving files tracked by git, prefer git mv over mv
- when creating git branches, always prefix with `jsonp--` and keep the total branch name to 36 characters or fewer
